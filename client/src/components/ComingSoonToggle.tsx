import { motion } from 'framer-motion';
import { useComingSoonMode } from '@/hooks/useComingSoonMode';

const ComingSoonToggle = () => {
  const { isComingSoonMode, disableComingSoonMode } = useComingSoonMode();

  return null;
};

export default ComingSoonToggle;